# Local K3D Configuration

## `general.yaml`

```yaml
cloud:
  local: {}
```

## `secrets.yaml`

No specific cloud credentials required for local K3D, but common secrets (Git, etc.) are still needed.

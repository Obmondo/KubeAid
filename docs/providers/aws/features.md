# AWS Features

## IAM Roles for Service Accounts (IRSA)

KubeAid supports IAM Roles for Service Accounts (IRSA) on AWS. This allows you to associate an IAM role with a Kubernetes service account. This service account can then provide AWS permissions to the containers in any pod that uses that service account.

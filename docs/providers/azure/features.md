# Azure Features

## Workload Identity

KubeAid supports Azure Workload Identity. This allows you to access Azure resources from your Kubernetes cluster without needing to store secrets in the cluster.

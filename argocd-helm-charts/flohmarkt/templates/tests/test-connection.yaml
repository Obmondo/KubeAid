apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "flohmarkt.fullname" . }}-test-connection"
  labels:
    {{- include "flohmarkt.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "flohmarkt.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

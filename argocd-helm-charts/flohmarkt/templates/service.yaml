apiVersion: v1
kind: Service
metadata:
  name: {{ include "flohmarkt.fullname" . }}
  labels:
    {{- include "flohmarkt.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "flohmarkt.selectorLabels" . | nindent 4 }}

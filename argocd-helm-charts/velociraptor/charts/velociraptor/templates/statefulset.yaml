apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "velociraptor.fullname" . }}
  labels:
    {{- include "velociraptor.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  serviceName: {{ include "velociraptor.fullname" . }}-gui
  podManagementPolicy: OrderedReady
  selector:
    matchLabels:
      {{- include "velociraptor.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/config: {{ include "velociraptor.configSecretName" . }}-{{ .Values.config.secretKey }}
        {{- /* Content checksums, so a changed artifact or overlay rolls the pod: the server
             reads artifact_definitions_directories only at startup, and a ConfigMap-only
             change would otherwise leave the old definitions running. */}}
        {{- if and .Values.customArtifacts.enabled (not .Values.customArtifacts.existingConfigMap) }}
        checksum/custom-artifacts: {{ include (print $.Template.BasePath "/configmap-custom-artifacts.yaml") . | sha256sum }}
        {{- end }}
        {{- if eq (include "velociraptor.overlayEnabled" .) "true" }}
        checksum/config-overlay: {{ include (print $.Template.BasePath "/secret-config-overlay.yaml") . | sha256sum }}
        {{- end }}
        {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "velociraptor.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: master
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      serviceAccountName: {{ include "velociraptor.serviceAccountName" . }}
      automountServiceAccountToken: {{ .Values.serviceAccount.automountServiceAccountToken }}
      {{- with .Values.hostAliases }}
      hostAliases:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- /* KubeAid patch: apiClient.imagePullSecrets for the publisher image. */}}
      {{- $pullSecrets := .Values.imagePullSecrets }}
      {{- if .Values.apiClient.enabled }}
      {{- $pullSecrets = concat (.Values.imagePullSecrets | default list) (.Values.apiClient.imagePullSecrets | default list) }}
      {{- end }}
      {{- with $pullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.priorityClassName }}
      priorityClassName: {{ . }}
      {{- end }}
      terminationGracePeriodSeconds: {{ .Values.terminationGracePeriodSeconds }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      {{- $init := include "velociraptor.overlayInitContainer" . }}
      {{- /* KubeAid patch: API client bootstrap after the config merge. */}}
      {{- $init = print $init (include "velociraptor.apiClientInitContainers" .) }}
      {{- if trim $init }}
      initContainers:
        {{- $init | nindent 8 }}
      {{- end }}
      containers:
        - name: velociraptor
          image: {{ include "velociraptor.image" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          args:
            - --config
            - {{ .Values.config.mountPath }}
            - frontend
            - -v
            {{- with .Values.extraArgs }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          ports:
            - name: frontend
              containerPort: {{ .Values.frontend.containerPort }}
              protocol: TCP
            - name: gui
              containerPort: {{ .Values.gui.containerPort }}
              protocol: TCP
            {{- if eq (include "velociraptor.apiExposed" .) "true" }}
            - name: api
              containerPort: {{ .Values.frontend.apiPort }}
              protocol: TCP
            {{- end }}
            {{- if .Values.serviceMonitor.enabled }}
            - name: metrics
              containerPort: {{ .Values.monitoring.containerPort }}
              protocol: TCP
            {{- end }}
          {{- with .Values.extraEnv }}
          env:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.livenessProbe }}
          livenessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.readinessProbe }}
          readinessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            {{- include "velociraptor.configVolumeMount" . | nindent 12 }}
            {{- include "velociraptor.customArtifactsVolumeMount" . | nindent 12 }}
            - name: datastore
              mountPath: {{ .Values.persistence.mountPath }}
            {{- if .Values.tmpDir.enabled }}
            - name: tmp
              mountPath: /tmp
            {{- end }}
            {{- with .Values.extraVolumeMounts }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
      volumes:
        {{- include "velociraptor.configVolumes" . | nindent 8 }}
        {{- include "velociraptor.customArtifactsVolume" . | nindent 8 }}
        {{- if .Values.apiClient.enabled }}
        {{- include "velociraptor.apiClientVolumes" . | nindent 8 }}
        {{- end }}
        {{- if .Values.tmpDir.enabled }}
        - name: tmp
          emptyDir:
            sizeLimit: {{ .Values.tmpDir.sizeLimit }}
        {{- end }}
        {{- if or (not .Values.persistence.enabled) .Values.persistence.existingClaim }}
        - name: datastore
          {{- if and .Values.persistence.enabled .Values.persistence.existingClaim }}
          persistentVolumeClaim:
            claimName: {{ .Values.persistence.existingClaim }}
          {{- else }}
          emptyDir: {}
          {{- end }}
        {{- end }}
        {{- with .Values.extraVolumes }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.topologySpreadConstraints }}
      topologySpreadConstraints:
        {{- toYaml . | nindent 8 }}
      {{- end }}
  {{- if and .Values.persistence.enabled (not .Values.persistence.existingClaim) }}
  volumeClaimTemplates:
    - metadata:
        name: datastore
        {{- with .Values.persistence.annotations }}
        annotations:
          {{- toYaml . | nindent 10 }}
        {{- end }}
      spec:
        accessModes:
          {{- toYaml .Values.persistence.accessModes | nindent 10 }}
        resources:
          requests:
            storage: {{ .Values.persistence.size | quote }}
        {{- if .Values.persistence.storageClass }}
        storageClassName: {{ .Values.persistence.storageClass | quote }}
        {{- end }}
  {{- end }}

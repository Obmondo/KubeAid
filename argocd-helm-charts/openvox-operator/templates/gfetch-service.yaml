{{- if .Values.gfetch.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-gfetch
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app.kubernetes.io/component: gfetch
spec:
  ports:
    - name: http-metrics
      port: 8080
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/component: gfetch
{{- end }}

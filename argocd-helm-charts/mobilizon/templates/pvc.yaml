apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "mobilizon.fullname" . }}-uploads
  namespace: mobilizon
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.persistence.uploads.size }}
